[pytest]
testpaths = tests
# Add the main repo root (PU_AISandbox/) to sys.path so that src.* imports resolve.
# This plugin lives at plugins/translation/, so two levels up is the repo root.
pythonpath = ../..
